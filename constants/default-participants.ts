/**
 * Default participants data
 * App starts with 0 participants - users add participants manually or via bulk import
 */

import { Participant } from "@/types";

export const DEFAULT_PARTICIPANTS: Participant[] = [];

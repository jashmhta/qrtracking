/** @type {const} */
const themeColors = {
  primary: { light: '#F5841F', dark: '#FF9F45' }, // Saffron Orange
  background: { light: '#FFFFFF', dark: '#09090B' }, // Zinc 950 for dark
  surface: { light: '#FAFAFA', dark: '#18181B' }, // Zinc 50 / Zinc 900
  foreground: { light: '#18181B', dark: '#FAFAFA' }, // Zinc 900 / Zinc 50
  muted: { light: '#A1A1AA', dark: '#71717A' }, // Zinc 400 / Zinc 500
  border: { light: '#E4E4E7', dark: '#27272A' }, // Zinc 200 / Zinc 800
  success: { light: '#10B981', dark: '#34D399' }, // Emerald
  warning: { light: '#F59E0B', dark: '#FBBF24' }, // Amber
  error: { light: '#EF4444', dark: '#F87171' }, // Red
  accent: { light: '#D4A84B', dark: '#D4A84B' }, // Golden
};

module.exports = { themeColors };